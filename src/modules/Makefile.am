INCLUDES = -DMODULES_PATH=\"$(datadir)/soma\"

noinst_LTLIBRARIES = libmodules.la

EXTRA_DIST = modules.h module.h

libmodules_la_SOURCES = modules.c

inc_DATA = module.h
incdir = $(includedir)/@PACKAGE@

